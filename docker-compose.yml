# Talkbox monorepo — root compose.
# Delegates to the kiosk stack in app/ (postgres + backend + frontend).
# Run everything from the repo root:
#   ./talkbox update        (or: docker compose up -d --build)
name: talkbox

include:
  - path: app/docker-compose.yml
    env_file: app/.env
