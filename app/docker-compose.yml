services:
  talkbox-postgres:
    container_name: talkbox-postgres
    image: pgvector/pgvector:pg18
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-talkbox}
      POSTGRES_USER: ${POSTGRES_USER:-talkbox}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-talkbox} -d ${POSTGRES_DB:-talkbox}"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    networks:
      - talkbox-network

  talkbox-backend:
    container_name: talkbox-backend
    image: ${IMAGE_BACKEND:-ghcr.io/la-plas-growth/talkbox-backend:latest}
    build:
      context: ./backend
      target: api
    ports:
      # Loopback-only: host tooling can hit the API directly at 127.0.0.1:8085
      # while external traffic keeps flowing through nginx on the frontend.
      - "127.0.0.1:8085:8000"
    env_file: .env
    volumes:
      - backend-data:/data
      - ./database/sacramento.db:/data/sacramento.db:ro
    depends_on:
      talkbox-postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8000/api/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - talkbox-network

  talkbox-frontend:
    container_name: talkbox-frontend
    image: ${IMAGE_FRONTEND:-ghcr.io/la-plas-growth/talkbox-frontend:latest}
    build:
      context: ./frontend
      args:
        - VITE_API_URL=${VITE_API_URL:-}
        - VITE_APP_NAME=${VITE_APP_NAME:-Talk Box}
    ports:
      - "127.0.0.1:8084:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      talkbox-backend:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:80/ > /dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
    networks:
      - talkbox-network

networks:
  talkbox-network:
    name: talkbox-network

volumes:
  postgres-data:
    name: talkbox-postgres-data
  backend-data:
    name: talkbox-backend-data
