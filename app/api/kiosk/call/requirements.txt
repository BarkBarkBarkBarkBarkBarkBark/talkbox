# Minimal deps for the dedicated kiosk-call function.
# Keep this list focused so the function stays well below Lambda bundle limits.
fastapi>=0.115
pydantic>=2.10
pydantic-settings>=2.7
python-multipart>=0.0.20
psycopg[binary]>=3.2
twilio>=9.5