import MarketingHomePage from "./MarketingHomePage.jsx";

/**
 * Always-on marketing preview (e.g. http://localhost:5173/site) so local
 * hosts can review the public site without spoofing hostname.
 */
export default function MarketingSitePage() {
  return <MarketingHomePage />;
}
