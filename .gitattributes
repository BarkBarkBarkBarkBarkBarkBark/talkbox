# Force LF for all shell scripts and Dockerfiles (must run in Linux containers/Pi)
*.sh        text eol=lf
Dockerfile  text eol=lf
*.conf      text eol=lf

# Default: auto-normalize everything else
*           text=auto
